<h1> CREATE YOUR CHEF <h2>

<%= form_for(@chef, url: {action: 'chef_created'}, method: :post) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit %>
    <br>
    <% @chef_images.each do |img_name| %>
        <%= radio_button("chef", "image_name", img_name) %>
        <%= image_tag(img_name)%>
        <br>
    <% end %>
<% end %>




